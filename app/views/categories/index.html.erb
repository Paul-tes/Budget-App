<div id="categories" class="body-height custom-light-gray-bg">
  <% @categories.each do |category| %>
    <%= render category %>
  <% end %>
  <%= link_to "Add category", new_category_path(user_id: current_user), class: "px-4 py-2 fixed bottom-0 text-center w-full font-semibold text-sm bg-cyan-600 text-white rounded-none shadow-sm hover:bg-cyan-700" %>
</div>

